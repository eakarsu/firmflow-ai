import{NextRequest}from'next/server';import prisma from'@/lib/prisma';import{apiActor,failure,response}from'@/lib/governance/http';import{accessibleMatterWhere,requireAccess}from'@/lib/governance/policy';
export async function GET(){try{const actor=await apiActor();return response(await prisma.timeEntry.findMany({where:{case:accessibleMatterWhere(actor.id,actor.role)},include:{case:{include:{client:true}},user:{select:{id:true,name:true}}},orderBy:{date:'desc'}}))}catch(error){return failure(error)}}
export async function POST(request:NextRequest){try{const actor=await apiActor();const body=await request.json();await requireAccess(prisma,actor.id,body.caseId,'EDITOR');return response(await prisma.timeEntry.create({data:{caseId:body.caseId,userId:actor.id,description:body.description,date:new Date(body.date),hours:Number(body.hours),billable:body.billable!==false,billingRate:Number(body.billingRate||0)}}),201)}catch(error){return failure(error)}}
