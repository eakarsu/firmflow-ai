import{NextResponse}from'next/server';export async function POST(){return NextResponse.json({error:'Generic drafting retired; use /api/governance/documents with AI_DRAFT provenance and independent counsel review'},{status:410})}
